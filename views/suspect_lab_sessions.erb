<%= erb :header %>

<div class="container">
    <h2>Suspect Lab Sessions</h2>
    <table class="table table-striped table-bordered table-condensed">
      <tr>
        <th>Student</th>
        <th>Time In</th>
        <th>Time Out</th>
        <th>Project Hours</th>
        <th>Signed Out By</th>
        <th>Excluded From Total</th>
        <th>Notes</th>
        <th>Action</th>
      </tr>
      <% LabSession.where(excluded_from_total: false).all.select { |s| s.student && s.duration_hours > 18 }.
        sort_by(&:duration_hours).reverse.each do |session| %>
        <tr>
          <td><%= session.student.first_name %> <%= session.student.last_name %></td>
          <td><%= session.time_in.in_time_zone("America/Los_Angeles").strftime("%D %l:%M %p") rescue "" %></td>
          <td>
            <%= session.time_out.in_time_zone("America/Los_Angeles").strftime("%D %l:%M %p") rescue "" %>
          </td>
          <td><%= session.duration_hours.round(1) %></td>
          <td><%= session.mentor ? "#{session.mentor.first_name} #{session.mentor.last_name}" :
              session.mentor_name %></td>
          <td><%= session.excluded_from_total %></td>
          <td><%= Rack::Utils.escape_html(session.notes) %></td>
          <% if @user.has_permission?("HOURS_EDIT") %>
            <td>
              <span style="white-space: nowrap;">
                <a href="/lab_sessions/<%= session.id %>/edit" class="btn btn-primary btn-small">Edit</a>
                <a href="/lab_sessions/<%= session.id %>/delete" class="btn btn-danger btn-small">Delete</a>
              </span>
            </td>
          <% end %>
        </tr>
      <% end %>
    </table>

    <h2>Sessions Before Join Date</h2>
    <p>These sessions are excluded from all totals and cannot be edited or deleted. Correct the student's join date in Members instead.</p>
    <table class="table table-striped table-bordered table-condensed">
      <tr>
        <th>Student</th>
        <th>Join Date</th>
        <th>Time In</th>
        <th>Time Out</th>
        <th>Project Hours</th>
        <th>Notes</th>
      </tr>
      <% LabSession.all.select { |s| s.student && s.student.counted_before_join_date?(s.time_in) }.
        sort_by { |s| s.time_in }.reverse.each do |session| %>
        <tr>
          <td><%= session.student.first_name %> <%= session.student.last_name %></td>
          <td><%= session.student.join_date %></td>
          <td><%= session.time_in.in_time_zone("America/Los_Angeles").strftime("%D %l:%M %p") rescue "" %></td>
          <td><%= session.time_out.in_time_zone("America/Los_Angeles").strftime("%D %l:%M %p") rescue "" %></td>
          <td><%= session.duration_hours.round(1) %></td>
          <td><%= Rack::Utils.escape_html(session.notes) %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>

<%= erb :footer %>
